* del on params should set cursor on next thing
* = on lhs of = goes to definition
* space on lhs of = adds a param after current pos

* TextView returns all character position rects
  * TextEdit uses the character position rects to locate enter cursor

* ScrollBar
